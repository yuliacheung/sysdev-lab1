[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "greet-zhang"
version = "0.1.0"
description = "A simple greeting package"
authors = [{name = "Zhang San", email = "zhangsan@example.com"}]
dependencies = []
readme = "README.md"
license = {text = "MIT"}

[project.scripts]
greet = "greet.cli:main"
